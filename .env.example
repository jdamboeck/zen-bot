# =============================================================================
# zen-bot — example environment variables (copy to .env and fill in)
# =============================================================================
# Core also reads from env.json: botToken can be set there as fallback.
# Env vars take precedence over env.json where both exist.
# =============================================================================

# -----------------------------------------------------------------------------
# Core (required)
# -----------------------------------------------------------------------------
# Discord bot token. Required — set BOT_TOKEN or add botToken to env.json.
# BOT_TOKEN=your_discord_bot_token_here

# Command prefix for bot commands (e.g. #play, #ask).
# PREFIX=#

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug | info | warn | error
# LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# SQLite database path. Directory is created if missing.
# DB_PATH=data/zen-bot.db

# -----------------------------------------------------------------------------
# LLM (Gemini) — optional; leave LLM_GEMINI_API_KEY unset to disable #ask
# -----------------------------------------------------------------------------
# Gemini API key. If unset, LLM feature is disabled.
# LLM_GEMINI_API_KEY=

# Bot character/persona for LLM responses (long string; override in .env if needed).
# LLM_BOT_CHARACTER=

# Model name (e.g. gemini-3-flash-preview).
# LLM_MODEL=gemini-3-flash-preview

# Max length for #ask reply before truncation (Discord limit 2000).
# LLM_ASK_MAX_RESPONSE_LENGTH=1900

# Instruction appended to #ask requests (Discord-friendly, concise).
# LLM_ASK_APPEND_INSTRUCTION=

# -----------------------------------------------------------------------------
# Music
# -----------------------------------------------------------------------------
# Default volume 0–100.
# MUSIC_VOLUME=50

# Ms to wait in empty queue (or after last track) before leaving voice channel.
# MUSIC_LEAVE_TIMEOUT=60000

# PO token HTTP endpoint (for playback / extraction).
# PO_TOKEN_URL=http://127.0.0.1:4416

# PO token cache TTL in hours.
# PO_TOKEN_TTL_HOURS=6

# Retry count and delay (ms) when fetching PO token.
# PO_TOKEN_RETRIES=3
# PO_TOKEN_RETRY_DELAY=2000

# LLM track comments: interval between comments (ms), max message length, max chars per comment.
# MUSIC_LLM_COMMENT_INTERVAL_MS=120000
# MUSIC_LLM_MESSAGE_MAX_LENGTH=1950
# MUSIC_LLM_SINGLE_COMMENT_MAX_CHARS=200

# LLM instruction for generating track comments (short witty one-liners).
# MUSIC_LLM_TRACK_COMMENT_INSTRUCTION=

# -----------------------------------------------------------------------------
# Example feature (optional — for demo/development)
# -----------------------------------------------------------------------------
# EXAMPLE_GREETING=Hello
# EXAMPLE_MAX_GREETINGS=100
# EXAMPLE_COOLDOWN_MS=5000
# EXAMPLE_ENABLED=true
# EXAMPLE_VERBOSE=false
# EXAMPLE_SPECIAL_GREETINGS=Howdy,Hey there,Greetings
